pipeline {
    agent { label 'jenkins_agent' }
    stages {
        stage('Build') {
            steps {
                script {
                sh 'echo "hello"'
                }
            }
        }
    }
}
